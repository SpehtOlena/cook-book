import styled from 'styled-components';
import ButtonStyled from './ButtonStyled';

const SupperButton = styled(ButtonStyled)`
	font-size: 40px;
	background-color: aquamarine;
`

export default SupperButton